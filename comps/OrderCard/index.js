import React from "react";
import styled from "styled-components";

const OrderCardCont = styled.View`

`

const OrderCard = ({

}) => {
  return (
    <OrderCardCont>
      
    </OrderCardCont>
  )
}